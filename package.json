{
  "name": "bigods",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@11.2.0",
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
